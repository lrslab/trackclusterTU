# Examples

This folder contains small, runnable examples for the TU-focused CLI: `trackclustertu`.

- `tu_minimal/`: tiny BED6 input with expected outputs and a direct `trackclustertu cluster` example
- `tu_multisample/`: pooled two-sample manifest workflow with shared TU outputs and sample/group count tables
